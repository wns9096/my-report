# 토요일 발표 — 다섯 질문에 대한 답

교안 Day4 「내일 (토) 할 것」이 미리 생각해 오라고 한 다섯 개다.
숫자는 전부 `checks/run_all.py` 로 재현된다.

---

## ① 내 데이터에서 무엇을 발견했는가

**지원처의 경쟁도가 퍼널 끝까지 따라온다.**

| 공고 경쟁도 | 지원 건 | 서류 통과 | 면접 통과 | 최종 합격 |
| --- | ---: | ---: | ---: | ---: |
| 낮음 (~.35) | 1,403 | **23.4%** | 8.5% | 2.9% |
| 보통 (.35~.65) | 2,242 | 17.6% | 6.5% | 2.4% |
| 높음 (.65~) | 1,316 | **13.1%** | 4.5% | 1.8% |

서류에서 10.4%p 벌어지고, 그 순서가 면접과 최종 합격까지 같은 방향으로 이어진다.
한 단계에서만 갈리면 그 단계의 잡음일 수 있는데 세 단계가 같다.
칸마다 표본이 1,300건 이상이라 표본 부족으로 흔들리는 값도 아니다.

**말할 수 없는 것도 같이 말한다.** 경쟁이 낮은 공고는 정의상 붙기 쉬운 공고이기도
하다. 그래서 이 관계는 얽혀 있다. 다만 무엇을 희생했는지는 봤다 —
직무 적합도가 경쟁 낮음 0.505 · 경쟁 높음 0.519로 거의 같다.
"덜 맞는 자리로 내려갔다"는 흔적은 이 데이터에 없다.
연봉·회사 규모·성장 기회는 데이터에 없으므로 거기까지는 말하지 않는다.

부수적으로 하나 더 — **축 여섯 개 중 넷은 안 갈렸다.**
고용 형태 1.1%p · 과제 유무 2.0%p · 직무 3.2%p · 산업 4.7%p.
전부 5%p 하한 아래다. 이것도 결과이고 한계 절에 적었다.

---

## ② 손계산 대조에서 처음에 무엇이 틀렸는가

**이탈률이 틀렸다. 원인은 분모였다. 51.2% → 65.8%.**

7주차에 손으로 구한 값(365명 중 240명 이탈)과 앱 값이 안 맞았다.
교안이 시킨 순서대로 의심했다.

| 순서 | 확인한 것 | 결과 |
| --- | --- | --- |
| 1 그레인 | 지원자 520명 = applicants 표 520행 | 여기 아님 |
| 2 분모 | 판정 대상 471명 vs 손계산 365명 · **471 − 106 = 365** | **여기다** |
| 3 필터 | 첫 지원일과 start_date 차이 중앙값 0일 | 여기 아님 |
| 4 기간 | 둘 다 AS_OF 2025-12-31 기준 | 여기 아님 |

**무엇이 틀렸나.** 관측 60일이 지난 사람 전원(471명)을 판정 대상으로 잡았는데,
그 안에 **최종 합격자 106명**이 섞여 있었다. 합격하면 지원을 멈추므로 무활동
일수가 길다 — 가장 잘된 사람이 이탈로 세어졌다.
성공 종료는 이탈이 아니므로 분모에서 뺐다. 이 하나로 지표가 14.6%p 움직인다.

고친 자리는 [core/metrics.py](core/metrics.py) `churn_split()`, 대조 스크립트는
[checks/day2_crosscheck.py](checks/day2_crosscheck.py) 다.

**이번 주에 걸린 것은 이것 말고 다섯 더 있다** (나머지 둘은 ⑤에).

| 무엇 | 어떻게 알았나 |
| --- | --- |
| 깨진 파일에서 앱이 **죽었다** | 필수 컬럼을 지운 사본을 넣었더니 검증보다 계산이 먼저 돌아 KeyError. 차단으로 멈추는 것과 에러로 죽는 것은 다르다 — 죽으면 판단할 화면이 없다. 계산을 검증 뒤로 옮겼다 |
| 캐시가 **안 풀렸다** | `st.cache_data` 는 `_` 로 시작하는 인자를 캐시 키에서 뺀다. 인자 이름이 `_stamp` 라서 파일을 바꿔도 옛 값이 나왔다 |
| 인과 검사가 **내 코드의 문장**을 잡았다 | 자동 생성 장의 "…이기 때문이다". 조립할 때 연결어로 인과를 쓰는 게 자연스럽다 |
| 인과 검사가 **멀쩡한 문장**을 잡았다 | "인해"를 부분 문자열로 찾아서 "확**인해**야 한다"가 걸렸다. 검사가 오탐을 내면 사람이 검사를 끈다 — 앞 글자를 보는 패턴으로 바꿨다 |

---

## ③ 못 믿을 지표를 어떻게 감췄는가

**계산해 놓고 숨긴 것이 아니라, 계산 자체를 안 했다.**

```python
# core/metrics.py — trust_check()
reason = trust_check(sample=n, obs_days=d, fairness=f)
if reason:
    return {"판정": "무효", "사유": reason,
            "이전": None, "이후": None, "변화": None}   # 값을 담지 않는다
```

조건은 셋이고 분기는 하나다.

| 조건 | 값 | 근거 |
| --- | --- | --- |
| 표본 부족 | 30건 미만 | 30건이면 한 건이 3.3%p를 움직인다. 그보다 흔들리는 값은 보고할 값이 아니다 |
| 관측 부족 | 90일 미만 | 최종 단계까지 90%가 55일, 가장 늦은 건이 72일 걸렸다. 72일 + 여유 |
| 비교 불공정 | 두 구간의 조건이 다름 | 무작위 배정이 없으므로 다른 요인을 배제하지 못한다 |

지금 데이터에서 **3건**이 걸렸다 — 판정 카드 1(유효 구간 밖) · 코호트 2(10월·11월).
화면에서는 회색 막대에 `○ 판정 보류`만 뜨고 값이 없다.
PDF에서도 같다. **한쪽만 감추면 의미가 없다.**

만든 것과 동작하는 것은 다르므로 [checks/day3_trust.py](checks/day3_trust.py) 에서
확인한다 — 감춘 항목에 값이 남았는지 훑고, 판정 7분기를 일부러 입력해 다 도는지 본다.

---

## ④ 해석 장에 무엇을 썼는가 — 자동으로 못 쓰는 이유

**가른 기준은 하나다 — 이 문장이 틀렸을 때 누가 책임지는가.**

| 자동 (5장) | 사람 (3장) |
| --- | --- |
| 요약 · 방법 · 결과 · 전후 비교 · 한계 | 배경 · 해석 · 제안 |
| 사실이 틀린 것뿐 | 사람이 책임진다 |

해석 장에 쓴 것 중 자동으로 나올 수 없는 문장 셋:

1. **"프로그램 성과를 말할 때는 사람 기준을 쓴다."**
   코드는 20.4%와 2.38%를 둘 다 낼 수 있지만, 어느 쪽이 이 보고의 답인지는
   묻는 질문이 정한다. 질문은 데이터에 없다.

2. **"경쟁도와 통과율은 정의상 얽혀 있다."**
   상관은 코드가 낸다. 그 상관이 순환인지 아닌지는 도메인을 알아야 안다.

3. **"이 값 하나로 희생이 없다고 말할 수는 없다 — 연봉·회사 규모·성장 기회는
   데이터에 없다."** 없는 컬럼을 코드는 그리워하지 못한다.

제안 장에는 **하지 않을 것 4개**를 함께 적었다. 선택하지 않으면 제안이 아니라
보고다. 그중 하나가 "«무조건 경쟁 낮은 곳에 지원하라»고 하지 않는다" 인데,
발견을 그대로 지시로 바꾸지 않기 위해 넣었다.

빈칸을 자동으로 채우지 않는 것도 확인했다 — 사람이 안 쓰면 문서에
`(작성되지 않음)` 이 찍힌다. [checks/app_audit.py](checks/app_audit.py) 규칙 4.

---

## ⑤ `/앱점검` 에서 무엇이 걸렸고 왜 그것이 규칙인가

`~/.claude/commands/앱점검.md` 가 없어서 만들었다.
기계로 볼 수 있는 10개는 [checks/app_audit.py](checks/app_audit.py) 가 훑는다.

**걸린 것 둘 — 규칙 6 「강조 색은 설정에서만」, 규칙 1 「임계값에 근거」**

### 규칙 1 — 근거가 아니라 뜻만 적혀 있었다

`CHURN_GAP_DAYS = 60` 옆에 `# 60일 무활동 → 이탈` 이라고만 적혀 있었다.
그건 **정의**이지 근거가 아니다. "왜 60인가"에 답하지 못한다.

점검기가 이걸 통과시켰다 — 규칙이 「«근거» 또는 «주석» 중 하나만 있으면 통과」
였기 때문이다. **느슨한 점검은 안 하느니만 못하다.** 통과했다는 사실이
안심시키기 때문이다. «근거»를 반드시 요구하게 조이니 세 건이 걸렸다.

근거는 데이터에서 찾았다 — 아직 활동 중인 사람의 연속 활동 간격이
중앙값 10일 · 90%가 15일 · **최댓값 16일**이고 30일 넘는 간격은 0건이다.
60일은 그 최댓값의 3.75배라 활동 중인 사람이 우연히 걸릴 여지가 없다.
그래서 60일 무활동은 "쉬는 중"이 아니라 "그만둔 것"으로 읽어도 된다.

### 규칙 6 — PDF가 판정 색을 따로 갖고 있었다

PDF 생성기가 판정 색을 `pdf.set_text_color(179, 38, 30)` 으로 직접 썼다.
`core/config.py` 의 `COLORS["block"]` 과 같은 값이지만 출처가 갈려 있었다.

**왜 그것이 규칙인가.** 색이 판정을 전달하기로 정했으면 의미가 한 곳에서만
나와야 한다. 두 곳에 흩어지면 한쪽만 바뀌는 날이 오고, 그때부터 빨강은 아무 뜻도
없게 된다. 뜻이 없어진 강조 색은 진짜 위험할 때 아무도 안 본다.
`_rgb("block")` 로 고쳤다.

**점검기 자체도 틀렸다.** 처음에는 `#RRGGBB` 만 찾아서 RGB 튜플을 놓쳤다.
위반을 일부러 되돌려 넣어 감지되는지 확인하고 나서야 알았다 —
검증을 깨뜨려 보는 것과 같은 이유다. 지금은 둘 다 잡는다.

두 건 다 고친 뒤 다시 돌려 10개 전부 지킴이다.
나머지 8개는 처음부터 지킴이었다.

| # | 규칙 | 왜 규칙인가 |
| --- | --- | --- |
| 2 | 메시지에 숫자 | "실패"는 판단 재료가 아니다. 12건과 198건은 대응이 다르다 |
| 3 | 감춘 값이 남지 않음 | 화면에서만 감추면 문서로 새어 나간다 |
| 4 | 빈 장은 빈 채로 | 자동으로 채우면 아무도 빈 줄 모른다 |
| 5 | 인과 단정 표현 | 시스템이 뱉은 문장은 검증된 것처럼 보인다 |
| 7 | 계산에 현재 시각 없음 | 오늘과 내일 값이 다르면 재현이 안 된다 |
| 8 | 게이트 근거 | "확인함"은 근거가 아니다 |
| 9 | 차단 규칙은 적게 | 차단이 늘면 사람이 하나씩 끄기 시작한다 |
| 10 | 계산은 한 곳에서 | 화면과 문서가 갈리면 어느 쪽이 맞는지 알 수 없다 |

---

### 규칙 11·12·13 — 주간 정리를 읽고 규칙을 셋 더 세웠다

주간 정리에는 기계가 볼 수 있는 것이 세 개 더 있었다. 규칙으로 옮겼다.

| | 규칙 | 왜 기계가 봐야 하는가 |
| --- | --- | --- |
| 11 | 카드를 직접 쓴 HTML 로 그리지 않는다 | 테마를 바꾸면 손으로 쓴 색만 안 따라온다. 눈으로는 밝은 테마에서 안 보인다 |
| 12 | 근거가 값의 **뜻**이 아니라 **출처**를 말한다 | 규칙 1은 «근거»라는 낱말만 본다. "근거: 적당해서"도 통과한다 |
| 13 | 저장소에 재배포 못 하는 파일이 없다 | 폰트는 눈에 안 띄게 무겁고 라이선스가 딸려 온다 |

**셋 다 일부러 위반을 넣어 잡히는지 봤다.** 그리고 12번이 처음에 안 잡혔다 —
내가 넣은 위반이 허술해서 블록에 다른 단서가 남아 있었다.
**위반을 제대로 넣는 것도 연습이 필요하다.** 대충 넣고 "잡힌다"고 믿으면
점검기를 못 믿게 되는 것과 같아진다.

13번은 실제로 걸렸다 — `fonts/malgun.ttf` 가 `C:\Windows\Fonts` 의
**해시까지 같은 사본**이었다. Windows 에 딸려 오는 폰트라 재배포할 수 없고 25MB다.
지우고 배포처에는 `packages.txt` 로 나눔고딕(OFL)을 깔게 했다.

### 규칙 바깥에서 걸린 것 — 교안이 권한 위젯이 검사를 죽였다

주간 정리가 권한 `st.segmented_control` 로 축 선택을 바꿨더니,
`AppTest` 가 두 번째 상호작용에서 통째로 죽었다.
단일 선택 값(문자열)을 리스트처럼 훑어 첫 글자만 떼어 간다 (Streamlit 1.45.1).

되돌렸다. **위젯을 얻고 검사를 잃는 거래는 하지 않는다** —
검사 못 하는 화면은 만든 것과 동작하는 것이 다른지 알 방법이 없다.
왜 안 썼는지를 코드에 적어 뒀다. 안 적으면 다음 사람이 또 바꿔 본다.


## 한 장으로 줄이면

> 이번 주에 배운 것은 "숫자를 내는 법"이 아니라 **"내 숫자를 못 믿는 법"** 이다.
>
> 깨뜨려 넣어 봤고(화), 손으로 센 것과 맞춰 봤고(수), 못 믿을 것은 계산조차 안 했고(목),
> 자동으로 쓴 문장을 코드가 다시 검사하게 했다(금).
> 네 번 다 무언가 걸렸다. 한 번에 맞았다면 오히려 확인을 안 한 것이다.
>
> 그리고 마지막에 **검사하는 쪽도 두 번 틀렸다.**
> 색 점검기가 RGB 튜플을 놓쳤고, 근거 점검기가 «뜻»을 «근거»로 받아 줬다.
> 확인하는 도구도 확인해야 한다 — 통과했다는 사실이 사람을 안심시키기 때문이다.
