# ★ 채운 자리 — Day별 정리

교안 Day1 프롬프트 2("★ 표시를 전부 찾아서 Day별로 정리해줘")에 대응하는 문서다.

배포된 골격 zip이 이 컴퓨터에 없어서 골격부터 직접 지었다.
그래서 "빈자리를 찾아 채웠다"가 아니라 **"어느 자리가 어느 날의 결정인지"** 를
여기에 남긴다. 다음에 다른 주제로 옮길 때 고칠 곳이 곧 이 목록이다.

## 도메인을 바꿀 때 고쳐야 하는 자리 — 13곳

| # | Day | 자리 | 무엇을 정하는가 |
| --- | --- | --- | --- |
| 1 | 화 | `core/config.py` `DATASET` · `PERIOD` · `AS_OF` | 데이터셋 이름 · 기간 · 기준일 |
| 2 | 화 | `core/config.py` `TABLES` | 읽을 파일 이름 (확장자 제외) |
| 3 | 화 | `core/config.py` `FUNNEL_STEPS` · `NON_FUNNEL` | 획득 퍼널 단계와 뺀 단계 |
| 4 | 화 | `core/validate.py` `REQUIRED` | 없으면 계산이 불가능한 컬럼 |
| 5 | 화 | `core/validate.py` `MIN_ROWS` · `OUT_OF_PERIOD_BLOCK` · `MISSING_WARN` | 차단/경고 임계값과 근거 |
| 6 | 수 | `core/config.py` `GRAIN` | 무엇을 하나로 셀 것인가 |
| 7 | 수 | `core/config.py` `RETENTION_STEPS` · `RETENTION_WEEKS` | 유지 단계 — 주어지지 않아 직접 정의 |
| 8 | 수 | `core/config.py` `CHURN_GAP_DAYS` · `JUDGE_MIN_DAYS` | 이탈의 정의 |
| 9 | 수 | `core/config.py` `THRESHOLDS` | 지표별 경고·위험 값과 근거 |
| 10 | 수 | `core/config.py` `VALID_UNTIL` | 유효 구간 — 우측 절단을 어디서 자르는가 |
| 11 | 목 | `core/config.py` `MIN_SAMPLE` · `MIN_OBS_DAYS` | 못 믿을 조건 |
| 12 | 목 | `core/config.py` `DECOMP_AXIS` · `CARD_AXIS` + `core/metrics.py` `AXES` | 무엇으로 쪼갤 것인가 |
| 13 | 금 | `report/sections.py` `BANNED_PATTERNS` | 이 도메인에서 금지할 표현 |

12번 중 `core/config.py` 밖에 있는 것이 하나 있다 — `metrics.AXES` 는 축 이름을
실제 컬럼에 잇는 표다. 컬럼 이름은 계산 코드 안에도 박혀 있어서, 설정 파일 하나만
고치면 되는 것이 아니다. 교안 1-3이 말한 그 지점이다.

## 날짜별로 무엇이 자랐나

| Day | 앱이 얻은 것 | 새로 생긴 파일 |
| --- | --- | --- |
| 화 | 데이터가 읽히고 깨진 파일이 차단된다 | `core/{config,loader,validate,gates}.py` · `screens/1_run.py` |
| 수 | 퍼널·유지·지표가 화면에 뜬다 | `core/metrics.py` · `viz/charts.py` · `screens/2_dashboard.py` |
| 목 | 못 믿을 지표는 숫자가 안 보인다 | `core/verdict.py` · `core/context.py` |
| 금 | 리포트 PDF가 나온다 | `report/{sections,pdf}.py` · `screens/3_report.py` |
| — | 통과 기록이 쌓인다 | `screens/4_archive.py` · `outputs/gates.jsonl` |

## 다 채우고 나면 지워도 되는 것

교안 부록 B-3이 말한 골격 전용 장치들이다.

- `core/todo.py` — **이미 지웠다.** 안 채운 자리에 안내 카드를 띄우는 모듈이었는데
  채울 자리가 남지 않아 쓰이지 않게 됐다.
- 화면의 `★ … 준비` 안내 카드 — 게이트를 아직 안 지난 화면에서만 뜬다.
  9주차에 배포할 때는 남겨 둔다. 처음 여는 사람에게 순서를 알려주는 역할이 있다.

---

## 옮기는 순서 — 설정 파일을 다 채워도 끝이 아니다

```
1. config.py 의 FUNNEL_STEPS · TABLES · PERIOD 를 내 것으로
2. loader.py 가 내 파일을 읽게
3. 앱을 켠다            → 깨진다        ← 정상이다
4. metrics.py 의 컬럼명을 하나씩 맞춘다
5. validate.py 에서 안 맞는 검증을 뺀다
```

**3번에서 깨지는 것이 정상이다.** 컬럼 이름이 계산 코드 안에도 박혀 있기 때문이다.
`config.py` 를 다 채우고 "끝났다"고 판단하면 거기서 막힌다.
위 13곳 중 12번(`metrics.AXES`)과 4번(`validate.REQUIRED`)이 그 자리다.

## 조용히 틀리는 자리 여섯

에러가 나면 알아서 고치게 된다. 무서운 것은 **에러가 안 나고 숫자만 틀리는 것**이다.

| 걸리는 곳 | 증상 | 어떻게 |
| --- | --- | --- |
| 분해 축이 남의 것인 채 | 축 목록에 내 도메인에 없는 값이 남아 분해가 안 됨 | `metrics.AXES` 까지 봐야 한다. `config.py` 에만 있는 게 아니다 |
| 최소 표본이 남의 것 | 칸이 전부 감춰지거나, 반대로 하나도 안 걸림 | 내 칸 크기를 세고 그 범위에서 고른다. 교안의 1,000은 이 도메인에 과했다 |
| `CLAUDE.md` 가 낡음 | "미정"이라 적어 둔 것이 이미 정해져 있음 | 규칙을 덧붙이기 전에 어긋난 곳부터 찾는다 |
| 게이트 근거가 예시 문구 | 회색 안내문을 그대로 저장 | 9주차에 이게 그대로 거버넌스 근거가 된다 |
| **팬아웃 조인** | 한 대상에 행이 여럿인 표를 그냥 조인 → 평균이 왜곡 | 대상 단위로 먼저 집계한 뒤 조인. `metrics.merge_1to1()` 이 행이 늘면 멈춘다 |
| **원본 값 ≠ 표시 이름** | 현업이 부르는 이름으로 원본을 고침 | 원본은 두고 `FUNNEL_LABELS` 에서만 바꾼다 |

아래 둘이 특히 조용하다. 그래서 팬아웃은 가드를 코드로 박고
(`checks/day3_trust.py` 가 양방향으로 확인한다), 표시 이름은 따로 뒀다.

## 내 도메인에 없으면 빼는 것 — 억지로 채우지 않는다

| 없으면 | 어떻게 |
| --- | --- |
| A/B 실험 | 전후 비교 카드로 대체한다. 이 앱이 그렇게 했다 |
| 획득 경로 | 그 함수를 지운다 |
| 이탈 개념 | 재이용률 · 반복 요청률로 바꾼다 |
| 비용 | 투입 공수(인시)로 바꿔도 된다 |
| 가드레일 | 카드에 "가드레일 없음 — 무엇을 희생했는지 확인되지 않음"을 적는다. 이 앱의 판정에도 그 분기가 있다 |

**빼면 그 사실을 방법 장에 적는다.** 안 적으면 읽는 사람은
"했는데 결과가 안 좋아서 뺐다"고 읽는다.

## 데이터가 적어도 된다

교안 예시는 9개 테이블 1,463,518행이지만, 이 앱은 **4개 파일 12,319행**이다. 다 된다.
조인할 표가 적으면 조인 함정도 적다. 문제가 되는 건 **나중에 쪼갤 때**다 —
칸마다 표본이 남는지가 관건이고, 그래서 `MIN_SAMPLE` 을 내 칸 크기에서 잡았다.
