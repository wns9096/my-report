# 비밀번호는 코드에 절대 쓰지 않는다. st.secrets 로 읽고, 파일은 올리지 않는다.
.streamlit/secrets.toml

__pycache__/
*.pyc

# 산출물은 다시 만들면 된다. 다만 게이트 기록은 근거라서 남긴다.
fonts/*.ttf

outputs/*.png
outputs/*.pdf
# doc_print.py 가 종이 폭을 재려고 잠깐 만드는 자리. 산출물이 아니다.
outputs/_print/
!outputs/gates.jsonl
