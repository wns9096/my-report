# 판단 기준 한 장

도메인 단어가 없는 문장만 남긴다. 업종·데이터 이름이 남아 있으면 그건 예시다.
혼자 자기 앱을 만들 때 이 장만 보면 된다.

각 날짜의 「내가 내린 결정」에는 도메인이 들어간다. 그건 기록이지 기준이 아니다.

---

## 화 — 앱이 데이터를 거부할 수 있는가

**배운 것**

- 차단과 경고를 가르는 것은 "계산이 틀리는가"다.
  틀린다 → 차단. 해석만 조심하면 된다 → 경고.
- 검증 규칙은 적게 만든다. 차단을 늘리면 아무것도 못 돌리고 결국 검증을 끄게 된다.
- 임계값에는 근거를 적는다. 근거 없는 임계값은 남의 것이다.
  두 달 뒤 "왜 그 값이죠?"에 답할 수 있어야 한다.
- 메시지에는 숫자를 넣는다. "실패"는 판단 재료가 아니다.
  얼마나 모자란지가 보여야 대응이 갈린다.
- 결측은 오류일 수도 구조일 수도 있다. 구조면 채우지 않는다. 채우면 위조다.
- 검증은 만든 것으로 끝나지 않는다. 깨뜨려 넣어 실제로 막히는지 본다.
- **차단으로 멈추는 것과 에러로 죽는 것은 다르다.** 죽으면 사람이 판단할 화면이 없다.
  그래서 계산은 검증 뒤에 둔다.

**내가 내린 결정**

| 무엇 | 판정 | 근거 |
| --- | --- | --- |
| 행 수 200 미만 | 차단 | 최소 표본 30 × 쓸 만한 축의 칸 수 6 = 180. 이보다 적으면 쪼개는 순간 전부 표본 부족이 된다 |
| 필수 컬럼 없음 | 차단 | 식별자·날짜가 빠지면 비율의 분모가 사라진다. 조심해서 해석할 여지가 없다 |
| 기간 밖 20% 초과 | 차단 | 다섯 중 하나가 밖이면 파일이 잘못 온 것이다. "그 행만 빼기"가 성립하지 않는다 |
| 기간 밖 20% 이하 | 경고 | 그 행만 빼고 진행할 수 있다. 분모가 얼마나 줄었는지만 알면 된다 |
| 결측률 5% 이상 | 경고 | 그 컬럼으로 쪼갤 때 (미분류) 칸이 생긴다. 몇 건인지 알고 있어야 한다. 채우지는 않는다 |
| 키 중복 · 값이 같음 | 경고 | 고유값으로 세면 사라진다. 다만 원본 행 수와 첫 단계가 어긋나는 이유이므로 알아야 한다 |
| 키 중복 · 값이 다름 | 차단 | 어느 행을 쓸지 정할 수 없다. 계산이 틀린다 |

차단은 셋만 만들었다. 뒤의 둘은 경고 전용이라 앱을 멈추지 않는다 —
멈추지 않는 규칙은 사람이 끄지 않는다.

교안 부록 A의 공통점이 그대로 들어맞았다.
**차단 = 키가 없거나 · 중복이거나 · 순서가 역행하거나 · 값이 불가능하다.
경고 = 비어 있거나 · 적거나 · 치우쳐 있다.**

깨뜨려 본 것 — 필수 컬럼을 지운 사본과 날짜를 1년 옮긴 사본을 넣었다.
둘 다 차단이 뜨고 통과 버튼이 잠겼다. 원본으로 되돌리니 풀렸다.
첫 시도에서는 앱이 에러로 죽었다. 검증보다 계산이 먼저 돌고 있었다.

---

## 수 — 내 앱의 숫자가 맞는가

**배운 것**

- 같은 데이터라도 무엇을 하나로 세느냐에 따라 다른 숫자가 나온다.
  둘 다 맞는 숫자다. 다른 질문에 답할 뿐이다.
- 숫자가 나왔다고 맞는 것이 아니다. 손으로 센 것과 대조해야 안다.
  대조할 값이 없으면 표본 100건으로도 충분하다. 자릿수만 맞으면 된다.
- 안 맞으면 **그레인 → 분모 → 필터 → 기간** 순으로 의심한다.
- 앞 단계를 안 거치고 온 대상이 많으면 퍼널이 아니라 분류다.
  분류를 퍼널이라 부르고 전환율을 내면 안 된다. 분모가 앞 단계가 아니기 때문이다.
- 관측 기간이 다른 것을 누적값으로 비교하지 않는다.
  최근 것이 낮은 건 성과가 아니라 시간의 문제일 수 있다.
- 유지는 단계가 주어지지 않는다. 내가 정의하고, 정의한 근거를 적는다.
- 성공적으로 끝난 대상을 이탈로 세지 않는다. 분모에서 빼야 한다.

**내가 내린 결정**

- 획득 퍼널의 그레인은 **대상 하나(사람)**. 답하려는 질문이 "이 대상이 결국
  전환했는가"라서다. 건 기준도 나란히 낸다 — 대조용이고, 답이 다른 질문이다.
- 유지 퍼널 단계는 후보 일곱을 늘어놓고 **지속 기간의 사다리** 넷을 골랐다.
  뒤 단계가 앞 단계를 구조적으로 함의하므로 퍼널이라 부를 수 있다.
  뺀 셋 — "재활동 2회 이상"은 전원이 해당해 단계가 안 갈리고,
  "중간 성과 경험"은 앞 단계를 안 거치고 나타나며,
  "성공 종료"는 유지가 아니라 종료다. 뒤 둘은 분류라서 구성비만 낸다.
  후보를 먼저 늘어놓고 **순서는 사람이** 정했다 — 순서까지 코드가 정하면
  판단할 것이 없어진다.
- 이탈은 성공 종료 없이 N일 무활동. N은 다음 활동까지의 실제 간격 분포에서 잡았다.

**★ 손계산 대조에서 처음 무엇이 틀렸나**

> 이탈률이 틀렸다. 원인은 **분모**였다.
> 관측 기간을 채운 대상 전원을 판정 대상으로 잡았는데, 그 안에 성공 종료한
> 대상이 섞여 있었다. 성공한 쪽은 활동을 멈추므로 무활동 일수가 길다 —
> 가장 잘된 대상이 이탈로 세어졌다. 51.2% → 65.8%.
> 그레인·필터·기간은 다 맞았고 분모 하나가 틀렸다.

---

## 목 — 무엇을 보여주지 않을 것인가

**배운 것**

- 못 믿을 조건이면 **계산하지 않는다.** 계산해 놓고 숨기는 것이 아니다.
  손에 없으면 못 쓴다. 그게 설계다.
- 사람은 본 숫자를 기억한다. 회색으로도 보여주지 않는다.
  대신 왜 못 믿는지는 자세히 적는다. 이유를 모르면 사람이 우회하려 한다.
- 표본이 적으면 유의성을 계산하지 않는다. 실제 값과 변화량만 본다.
  모른다는 것을 아는 것도 결론이다. 기준을 낮춰서 통과시키는 것이 가장 나쁘다.
- 판정 순서는 코드로 고정한다. 좋은 결과를 먼저 보면 뒤의 확인을 대충 하게 된다.
- 주지표가 올랐다고 성공이 아니다. 무엇을 희생했는지 함께 본다.
  가드레일이 없으면 판정하지 않는다.
- 쪼개는 축은 격차가 보이는 것이 아니라 **손을 쓸 수 있는 것**으로 고른다.
  격차가 없는 것도 결과다. 그 축으로는 안 갈린다는 것을 확인한 것이다.
- 색은 판정을 전달한다. 강조 색을 장식에 쓰면 진짜 경고를 아무도 안 본다.
  색 + 기호 + 글자를 같이 쓴다. 색만으로는 20명 중 1명이 못 본다.

**내가 내린 결정**

| 못 믿을 조건 | 값 | 근거 |
| --- | --- | --- |
| 표본 부족 | 30건 미만 | 30건이면 한 건이 3.3%p를 움직인다. 그보다 흔들리는 값은 보고할 값이 아니다 |
| 관측 부족 | 90일 미만 | 마지막 단계까지 90%가 55일, 가장 늦은 건이 72일 걸렸다. 그보다 짧으면 "아직 안 간 것"과 "못 간 것"을 구분할 수 없다 |
| 비교 불공정 | 두 구간의 조건이 다름 | 무작위 배정이 없으므로 다른 요인을 배제하지 못한다 |

셋이면 충분하다. 조건이 많으면 전부 걸려서 아무 지표도 안 보이고,
그러면 사람이 조건을 끄기 시작한다 — 차단 규칙을 적게 만든 것과 같은 이유다.

- 주지표가 "움직였다"의 하한은 **5%p**. 최소 표본 30에서 잡음만으로 생기는
  폭(3.3%p)의 1.5배다.
- 가드레일 악화는 **임계값 경고선 아래로 내려가거나 기준 대비 10% 이상 하락**.
- 분해 축은 여섯 후보 중 **격차가 가장 큰 것이 아니라 손을 쓸 수 있는 것**을 골랐다.
  가장 큰 축(12.5%p)은 우리가 바꿀 수 있는 값이 아니라서 뺐다 — 교안의 "날씨" 축이다.
  두 번째로 큰 축(10.4%p)은 우리가 고르는 값이라 골랐고, 칸마다 표본도 40배 많다.
  나머지 넷은 5%p 하한 아래였고 "이 축으로는 안 갈린다"로 한계에 적었다.
- 분해 축과 판정 카드 축의 **그레인이 다르다**. 분해는 "어디를 고칠까"라서 건 단위,
  카드는 "어느 집단이 다른가"라서 사람 단위다. 한쪽으로 통일하면 한쪽이 틀린다.
- 한 축에서만 갈리면 그 단계의 잡음일 수 있다. **뒤 단계까지 같은 방향인지** 본다.

---

## 금 — 자동으로 만든 문장이 거짓말을 하는가

**배운 것**

- 틀렸을 때 사람이 책임지는 문장은 사람이 쓴다.
  해석과 제안을 자동화하면 책임의 주체가 사라진다. **이것이 나흘의 결론이다.**
- 사람이 쓰는 장이 비어 있으면 비운 채로 내보낸다. 문서에 「작성되지 않음」이 찍힌다.
  자동으로 채우면 아무도 빈 줄 모른다.
- 관측한 데이터로는 인과를 주장하지 않는다.
  "A 때문에 B" 대신 "A가 낮은 구간에서 B도 낮다".
- 코드가 자기 문장을 검사한다. 사람이 쓴 장에도 건다. 사람이 더 자주 인과를 쓴다.
- 검증에서 난 경고는 반드시 한계로 옮긴다. 안 옮기면 그 경고는 없던 일이 된다.
- 찾았는데 없던 것도 적는다. "없음"도 결과다.
  안 적으면 다음 사람이 같은 것을 또 찾는다.
- 화면에서 감춘 숫자는 문서에도 없어야 한다. 한쪽만 감추면 의미가 없다.
- 되돌릴 수 없는 것은 화면이 그 무게를 보여줘야 한다. 다른 버튼과 같으면 안 된다.

**내가 내린 결정**

- 자동으로 쓰는 장: 요약 · 방법 · 결과 · 비교 · 한계.
  사람이 쓰는 장: 배경 · 해석 · 제안. 가른 기준은 "틀렸을 때 누가 책임지는가"다.
- 금지어에 도메인 표현을 더했다 — 견인 / 기여했 / 영향을 미쳤 / 유발 / 기인.
  상관을 인과로 바꾸는 말이라서다.
- 검사를 자동 장과 사람 장 **양쪽**에 걸었다.
  일부러 넣어 보니 양쪽 다 잡혔고, 되돌리니 사라졌다.
- 발송 게이트에는 확인 문구를 한 번 더 받게 했다. 되돌릴 수 없어서다.

**★ 검사가 처음 잡은 것은 내 코드가 쓴 문장이었다**

> 자동 생성 장의 "…이기 **때문이다**"가 걸렸다.
> 문장을 조립할 때 연결어로 인과를 쓰는 것이 자연스럽다.
> 그래서 코드가 자기 문장을 검사해야 한다.

**★ 그다음에는 검사가 멀쩡한 문장을 잡았다**

> 금지어를 부분 문자열로 찾았더니 "확**인해**야 한다"가 걸렸다.
> 검사가 오탐을 내면 사람이 검사를 끈다 — 차단 규칙을 적게 만드는 것과 같은 이유다.
> 앞 글자를 보는 패턴으로 바꾸고, 걸려야 할 문장과 안 걸려야 할 문장을
> 각각 넣어 두 방향을 다 확인했다.

---

## 나흘을 합치면 — 규칙 다섯

같은 말이 두 번 이상 나온 것을 하나로 합쳤다.

1. **판정을 붙이는 기준은 "이대로 가면 값이 틀리는가"다.**
   틀리면 막고(차단·무효), 해석만 조심하면 되면 흘려보낸다(경고).
   막는 것은 적게 만든다. 많으면 사람이 끄기 시작하고 검증 전체가 무력해진다.

2. **믿을 수 없으면 계산하지 않는다.**
   계산해 놓고 숨기는 것이 아니다. 값이 손에 없어야 못 쓴다.
   화면에서 감춘 것은 문서에서도 감춘다.
   감추는 대신 사유는 자세히 적는다 — 실제 값과 기준값을 넣어서.

3. **숫자에는 세는 단위와 근거가 따라붙는다.**
   무엇을 하나로 셌는지, 임계값이 왜 그 값인지, 분모가 무엇인지.
   적히지 않은 숫자는 두 달 뒤에 아무도 설명하지 못한다.

4. **만든 것과 동작하는 것은 다르다.**
   검증은 깨뜨려 넣어 본다. 숫자는 손계산과 대조한다.
   감추기는 걸리는 것을 확인한다. 검사는 일부러 넣어 본다.
   한 번에 맞으면 오히려 뭔가 안 바꾼 것이다.

5. **사실은 자동으로, 판단은 사람이.**
   값·비교·변화·조건은 코드가 쓴다.
   원인·우선순위·할 것·승인은 사람이 쓴다.
   빈칸은 빈칸으로 남긴다. 자동으로 채우면 책임의 주체가 사라진다.

---

## 규칙을 지키는지 누가 보는가

다섯 규칙을 세워 놓고 지키는지는 사람이 눈으로 봤다. 그러면 빠뜨린다.
그래서 기계로 볼 수 있는 것은 점검 스크립트로 옮겼다 — `checks/app_audit.py`.

| 기계가 보는 것 | 사람이 봐야 하는 것 |
| --- | --- |
| 임계값 옆에 근거 주석이 있는가 | 그 근거가 진짜 근거인가 |
| 메시지에 숫자가 있는가 | 차단과 경고가 제대로 갈렸는가 |
| 감춘 값이 남아 있는가 | 감출 조건이 도메인에 맞는가 |
| 빈 장이 자동으로 채워졌는가 | 사람이 쓴 장이 실제로 판단인가 |
| 금지 표현이 남아 있는가 | 쪼갠 축이 손을 쓸 수 있는 것인가 |
| 판정 색이 한 곳에서 나오는가 | 판정 순서가 코드로 박혀 있는가 |
| 계산에 현재 시각이 섞였는가 | 한계 절이 경고를 전부 물고 왔는가 |

**그리고 점검기도 점검한다.** 두 번 걸렸다.

- 색 점검기가 16진수만 찾아서 RGB 튜플로 쓴 위반을 놓쳤다.
  위반을 일부러 되돌려 넣어 감지되는지 보고 나서야 알았다.
- 근거 점검기가 «근거» 또는 «주석» 중 하나만 있으면 통과시켰다.
  그래서 값의 **뜻**만 적어 둔 항목이 새어 나갔다 — 뜻은 정의이지 근거가 아니다.
  "N일 무활동이면 이탈"은 정의이고, "활동 중인 사람의 간격이 최대 16일이라
  N일은 그 3.75배"가 근거다. 조이고 나니 세 건이 더 걸렸다.

**느슨한 점검은 안 하느니만 못하다.** 통과했다는 사실이 안심시키기 때문이다.
검사하는 쪽도 만든 것과 동작하는 것이 다르다 — 그래서 검사기에도 일부러 넣어 본다.

---

## 다시 세어 보니 여덟이었다

다섯으로 묶었던 것을 주간 정리와 맞춰 보니, 합치면서 두 가지를 잃었다.
**«순서»와 «없었다는 기록»** 이다. 둘 다 다른 규칙으로 대신할 수 없어서 되살렸다.

| | 규칙 | 이걸 어기면 |
| --- | --- | --- |
| ① | 차단과 경고를 가르는 질문은 하나다 — **이대로 계산하면 값이 틀리는가** | 다 차단하면 아무것도 못 돌리고, 다 경고하면 게이트가 장식이 된다 |
| ② | **세는 단위를 먼저 정한다** | 한 대상이 같은 단계를 두 번 밟으면 행을 세는 순간 전부 다시 만들어야 한다 |
| ③ | 근거 없는 임계값은 남의 것이다 — **어디서 온 값인지**를 적는다 | 두 달 뒤 "왜 그 값이죠?"에 아무도 답하지 못한다 |
| ④ | **못 믿으면 계산하지 않는다** | 변수에 남은 값은 리포트·로그·차트 어디로든 새어 나간다 |
| ⑤ | **판정 순서를 코드로 고정한다** | 좋은 결과를 먼저 보면 뒤의 확인을 대충 한다. 규율에 맡기지 않는다 |
| ⑥ | 관측 데이터로는 인과를 주장하지 않는다 | "A 때문에 B"는 사실이 아니라 주장이다. 약해 보이는 쪽이 정확하다 |
| ⑦ | 경고는 반드시 한계로 옮기고, **찾았는데 없던 것도 적는다** | 안 옮긴 경고는 없던 일이 되고, 안 적은 "없음"은 다음 사람이 또 찾는다 |
| ⑧ | 틀렸을 때 사람이 책임지는 문장은 사람이 쓴다 | 해석과 제안을 자동화하는 순간 책임의 주체가 사라진다 |

앞의 다섯과 겹치지만 같지 않다. ⑤와 ⑦이 다섯 안에서는 ④와 ②에 얹혀 있었는데,
얹힌 규칙은 점검할 때 빠진다 — **점검기에 규칙 하나로 옮길 수 없으면 합친 것이 아니라
잃은 것이다.** 실제로 ⑦의 "없었다는 기록"은 한계 절에서 한 번 빠질 뻔했다.

**이 여덟에는 도메인 단어가 하나도 없다.** 그게 남는 것인지 아닌지를 가르는 기준이다.

---

## 나흘째에 하나 더 — 도구가 안 건드린 곳은 안 본다

Claude 는 자기가 손댄 곳만 본다. 값을 여러 번 보고하면서도
그 값의 **단위**는 한 번도 말하지 않았다. 물어보지 않았기 때문이다.

그래서 시킬 때 여섯 가지를 붙인다. `CLAUDE.md` 에 넣어 두었다.

| 시키는 말 | 왜 |
| --- | --- |
| "내가 지정하지 않았는데 네가 정한 값을 전부 말해라" | 빈칸을 조용히 채우고, 채웠다고 말하지 않는다 |
| "1번 뒤에 2번을 두고, 1번을 통과했다고 바로 가지 마" | 순서를 안 박으면 계산 뒤에 판정을 숨기는 코드가 나온다 |
| "판단하지 말고 사실만" | 먼저 "이건 오류입니다"라고 말해 버리면 내가 판단할 기회가 없어진다 |
| "판정만 해라. 고치지는 마" | 발견과 수정을 한 번에 시키면 뭘 고쳤는지 모르고 지나간다 |
| "고치라고 한 파일 밖이 바뀌었으면 말해라" | 대개 선의다. 문제는 그 사이에 판정 로직이 섞일 때다 |
| 규칙은 스무 개를 넘기지 않는다 | 넘어가면 지켜지지 않기 시작한다. 그때는 줄이는 게 맞다 |

---

## 2026-09-09 — 이 분석을 근거로 쓸 수 있는가

지난주까지는 «숫자가 맞는가»를 물었다. 이번 주는 «이 숫자로 남을 움직일 수
있는가»다. **맞는 숫자인데 근거가 안 되는 것이 있다.**

**배운 것**

- 맞는 숫자와 근거가 되는 숫자는 다르다.
  정확해도 분모가 없으면 아무도 결정하지 못한다.
- 근거는 **조회한 것**이다. 요약에서 옮겨 적은 것이 아니다.
  요약을 요약하면 틀린 곳을 찾을 수 없게 된다. 그리고 요약은 어느 시점의 계산이다.
- 근거에는 넷이 있어야 한다 — **분자·분모 · 비교 대상 · 비중 · 표본**.
  비중을 모르면 그게 큰일인지 모른다.
- 같은 비율이라도 분모가 무엇이냐에 따라 다른 값이다. **어느 분모인지 적는다.**
  비중도 마찬가지다 — 전체 기준인지 그 구간 진입자 기준인지.
- 한 건이 바뀌어 값이 격차보다 크게 흔들리면 그 축으로는 결론을 못 낸다.
  분모가 100 미만인 칸은 비율로 쓰지 않고 건수로 쓴다.
- 최근 구간이 낮은 것은 대개 악화가 아니라 **시간이 안 찬 것**이다.
- 화면에서 감춘 값을 문서에 쓰면 감춘 것이 없던 일이 된다.
  감춘 항목은 버리는 것이 아니라 «무엇이 채워지면 판정 가능한가»를 적어 둔다.
- 비율이 아니라 평균인 지표는 **분자·분모가 없다.** 근거 문장으로 못 쓴다.
  가드레일로는 쓸 수 있다 — 가드레일은 «움직였나»만 보면 되니까.
- 응답한 사람의 평균은 전체의 평균이 아니다. 건의 평균도 대상의 평균이 아니다.
- 발견은 숫자가 아니라 **문장**이다. 값 · 비교 · 비중 세 줄이 있어야 한다.
- 발견에 원인을 섞지 않는다. 섞으면 나중에 그 문장을 정당화하게 된다.
- 전환율이 가장 낮은 구간과 이탈이 가장 많은 구간은 다를 수 있다.
  다르면 구멍이 두 개다. 그리고 **세는 단위를 바꾸면 병목이 옮겨 간다.**

**내가 내린 결정**

| 무엇 | 결정 | 왜 |
| --- | --- | --- |
| 버린 숫자 — 평균 지표 둘 | 근거로 안 쓴다 | 비율이 아니라 평균이라 분자·분모가 없다. 하나는 대상당 평균도 아니어서 다작한 대상에 기울어 있다 |
| 버린 숫자 — 감춘 항목 셋 | 값을 문서에 안 적는다 | 지난주에 관측 미달로 계산조차 안 했다. 꺼내 쓰면 감춘 의미가 없다 |
| 버린 숫자 — 안 찬 구간 포함 평균 | 유효 구간만 쓴다 | 두 값이 0.77%p 벌어진다. 어느 쪽을 썼는지 문서에 적었다 |
| 크기를 무엇으로 쟀나 | 격차 × 비중 + 절대 건수 환산 | 격차만 크면 고쳐도 전체가 안 바뀌고, 비중만 크면 고칠 것이 없다 |
| 파고들 순서 | 크기 1위를 1순위로 두지 않았다 | 1위 축은 우리가 바꿀 수 없는 값이다. 2위가 개입 가능하고 표본도 가장 튼튼하다 |
| 격차를 어떻게 뺐나 | 표시값끼리 뺀다 | 원값끼리 빼면 화면의 두 값으로 암산이 안 맞는다. 자릿수가 어긋나면 맞는 값도 틀린 것처럼 보인다 |

**★ 크기 표에서 1위가 파고들 1순위가 아니었다**

> 격차 × 비중이 가장 큰 축이 우리가 바꿀 수 없는 속성이었다.
> 표만 보고 순서를 정하면 «가장 큰 것»부터 파게 되는데, 그것은 손을 쓸 수 없는 곳이다.
> **격차 × 비중은 «얼마나 큰가»에 답하고, «무엇을 할 수 있는가»에는 답하지 않는다.**
> 그래서 크기 표와 파고들 순서를 따로 적고, 순서를 바꾼 이유를 남겼다.

**★ 조회 스크립트와 문서의 자릿수가 어긋났다**

> 스크립트는 격차를 원값끼리 빼서 10.4%p 로, 문서는 표시값끼리 빼서 10.3%p 로 적었다.
> 지난주에 화면 쪽에는 같은 규칙을 넣어 뒀는데 새로 만든 조회 스크립트에는 안 넣었다.
> **규칙을 한 곳에 넣는 것만으로는 안 된다 — 새로 만드는 것마다 다시 넣어야 한다.**
> 그래서 조회 스크립트에도 표시값 함수를 두고, 문서의 숫자를 그 함수가 만들게 했다.

---

## 2026-09-09 (Day2) — 이 제안을 앱에서 바로 볼 수 있는가

어제는 근거가 갖춰진 문장을 만들었다. 그 문장은 파일 안에만 있었다.
오늘은 그것을 화면에서 바로 만들고 바로 받아 갈 수 있게 옮겼다.
**명령을 돌리는 것과 기능을 만드는 것은 다르다.** 오늘은 후자다.

**배운 것**

- 문서는 자동으로 쓰는 절과 사람이 쓰는 절이 나뉜다.
  판단이 들어가는 절을 자동화하면 책임의 주체가 사라진다.
- 절을 나누는 기준은 하나다 — **다시 조회하면 똑같이 나오는가.**
  그렇다면 자동, 아니면 사람이다.
- 그 갈림을 코드가 들고 있어야 한다. 화면이 제목을 보고 나누면
  절이 하나 늘 때마다 화면도 고쳐야 하고, 그러다 한 번은 안 고친다.
- 카드에 없는 것을 화면에 채우지 않는다. 없으면 «미확인» 으로 정직하게 남긴다.
  채워서 보여주면 그럴듯해 보이지만, 아직 안 정한 것을 정한 것처럼 만든다.
- 원본은 하나다. 화면은 읽기만 한다 — 화면에서 값을 고치기 시작하면
  원본과 화면 중 어느 것이 진짜인지 알 수 없게 된다.
- 문장 검사는 화면에서도 걸어야 한다. 화면에서 지키고 문서에서 안 지키면 의미가 없다.
- 검사는 재사용한다. 같은 목록을 두 곳에 두면 한 곳만 고쳐지고,
  그때부터 두 문서가 다른 기준으로 검사된다.
- 본문 순서를 바꾸지 않는다. «할 것»을 먼저 두면 멈추자는 제안은 안 읽힌다.
- 후보는 자동으로 나열하고, 그중 무엇을 고를지는 사람이 정한다.
- 기능을 먼저 만들고 자리는 나중에 정한다.
  자리를 먼저 정하면 아직 없는 메뉴에 맞춰 코드를 짜게 되고, 시간의 절반을 배선에 쓴다.
- 효과를 확인하지 않은 것을 «할 것»에 넣지 않는다. 한 칸이 비는 것도 결과다.

**내가 나눈 절**

| 절 | 어느 쪽 | 왜 |
| --- | --- | --- |
| 한 장 요약 | 자동 | 원본의 근거와 분류를 옮긴 것뿐이다. 다시 조회하면 같다 |
| 분류 셋 (멈출 것·다시 볼 것·할 것) | 자동 | 원본을 분류별로 늘어놓는다. 판단은 원본을 고를 때 이미 했다 |
| 이 제안이 틀린다면 | 사람 | 무엇이 틀릴 수 있는지는 데이터가 모른다. 문서 전체에 하나만 둔다 |
| 적용 | 사람 (후보는 자동) | 후보는 기록에서 뽑히지만, 무엇을 볼지는 판단이다 |
| 부록 (근거 상세) | 자동 | 원본의 근거 줄을 다시 옮긴다. 계산은 어디서도 새로 하지 않는다 |

**내가 내린 결정**

| 무엇 | 결정 | 왜 |
| --- | --- | --- |
| 절 순서 | 멈추자는 제안을 맨 앞에 고정했다 | 뒤에 두면 안 읽힌다. 읽는 사람은 «할 것»에서 멈춘다 |
| 자동과 사람의 경계 | «다시 조회하면 같은가»로 갈랐다 | 제목으로 가르면 절이 늘 때마다 화면도 고쳐야 한다 |
| 못 채운 자리 | 지우지도 채우지도 않고 그대로 내보낸다 | 채우면 안 정한 것을 정한 것처럼 만든다 |
| 「할 것」 한 칸 | 비워서 내보냈다 | 효과를 실측한 것이 하나도 없다. 빈 칸이 오늘의 결과다 |
| 사람이 쓴 절을 어디 두나 | 이 세션에만 둔다 | 자리가 임시다. 파일로 남기면 내일 옮길 때 두 곳을 고쳐야 한다 |
| 내보낸 문서의 색 | 문서 서식도 설정에서 색을 받아 온다 | 파일 안에 색을 박으면 설정을 고쳐도 그 파일만 옛 색으로 남는다 |

**★ 새로 만든 파일은 지난 규칙을 모른다**

> 어제는 새 조회 스크립트가 표시값 규칙을 몰랐다. 오늘은 새 서식이 색 규칙을 몰랐다.
> 이틀 연속 같은 자리에서 걸렸다 — **규칙을 한 곳에 넣는 것만으로는 안 된다.**
> 기억에 맡기지 않으려면 기계가 새 파일까지 훑어야 한다.
> 그래서 점검에 «새로 만든 조립층도 같은 원칙을 따르는가»를 한 줄 더했다.

**★ 개수로 대조하면 자리를 검사가 정해 버린다**

> 못 채운 자리를 «개수»로 대조했더니 여섯과 다섯으로 갈렸다.
> 문서에는 여섯 곳 다 있었고, 한 곳이 다른 절에 나가 있었을 뿐이다.
> 개수를 세는 검사는 «어느 절에 나가야 하는지»까지 미리 정해 버린다.
> 자리를 묻지 말고 **그 문장이 문서에 있는가**를 물으면 된다. 양방향으로 본다.

---

## 2026-09-10 (Day3) — 이 제안서를 남의 책상에 그대로 올릴 수 있는가

어제는 문서를 화면에서 만들 수 있게 했다. 오늘은 그 문서를 읽은 사람이
**결정을 내릴 수 있는가**를 물었다. 읽고 «잘 봤다»로 끝나면 실패다.

**남는 것 다섯**

- 절은 데이터가 채울 수 있는 것만 만든다. 채울 함수를 못 대면 그 절은 없는 것이고,
  **자리를 비워 두는 것과 자리를 안 만드는 것은 다르다.** 비워 두면 그 빈칸이 인쇄된다.
- 절의 순서가 곧 설득의 순서다. 규모를 제안 뒤에 두면 «왜 해야 하는지»가 안 풀리고,
  위험을 요청 뒤에 두면 결정한 다음에 리스크를 듣는다.
- 주제는 만들 수 있는 만큼 만들어 놓고 고른다. 하나만 뽑으면 그날 눈에 띈 것이
  그대로 이번 분기의 우선순위가 된다. **기각한 후보도 목록에서 지우지 않는다** —
  지우면 «안 봤다»와 «보고 아니었다»가 구분되지 않는다.
- 모른다는 말은 셋이 갖춰져야 요청이 된다 — 무엇을 모르는가 · 누가 확인하는가 ·
  모르는 채로 할 수 있는 결정은 무엇인가. 낱말 하나로 두면 그냥 구멍이다.
- 요청 줄에 결정을 요구하는 동사가 없으면 그건 보고이지 제안이 아니다.

**오늘 나눈 절과 고른 말**

| 무엇 | 정한 것 | 왜 |
| --- | --- | --- |
| 절 여섯 | 현황 · 원인 · 규모 · 제안 · 위험 · 요청 | 읽는 사람의 머릿속을 지나가는 질문 순서다 |
| 자동 넷 | 현황 · 원인 · 규모 · 제안 | 다시 조회하면 같은 값이 나온다. 틀리면 사실이 틀린 것이다 |
| 사람 둘 | 위험 · 요청 | 언제 접을지, 무엇을 결정해 달라고 할지는 데이터가 못 정한다 |
| 만들지 않은 절 셋 | 실험 결과 · 경쟁사 비교 · 금액 환산 | 채울 것이 없다. 자리를 만들지 않았다 |
| 절 제목 | 읽는 사람이 던질 질문 그대로 | 제목이 질문이면 그 아래 문단이 자동으로 답이 된다 |
| 못 채운 자리 | 「확인 필요」 + 셋 | 낱말 하나는 구멍이고, 셋이 갖춰지면 요청이다 |

**내가 내린 결정**

| 무엇 | 결정 | 왜 |
| --- | --- | --- |
| 읽는 사람 | 나 자신으로 잡았다 | 이 자료로 결정을 내릴 사람이 나뿐이다. 가상의 결재자를 세우면 요청 절이 연기가 된다 |
| 격차 하한 | 새로 만들지 않고 판정에서 쓰던 값을 빌렸다 | 여기서 새로 정하면 판정과 주제 선정이 서로 다른 자를 쓰게 된다 |
| 비교 기간 길이 | 관측이 차는 데 걸리는 기간과 같게 뒀다 | 그보다 짧게 끊어 견주면 아직 안 찬 구간을 «떨어졌다»로 읽는다 |
| 규모의 단위 | 금액이 아니라 건수로 냈다 | 건당 금액을 적어 둔 항목이 없다. 평균을 지어내지 않는다 |
| 옛 자리 | 임시 탭을 지우기 전에 옮기고, 옮긴 뒤 차이를 눈으로 봤다 | 지우고 나서 옮기면 무엇이 빠졌는지 알 수 없다 |
| 그림 | 문서와 화면이 같은 그림 하나를 쓰게 했다 | 두 벌을 그리면 언젠가 두 그림이 서로 다른 말을 한다 |

**★ 같은 표가 화면과 문서에서 다른 값으로 보였다**

> 계산은 한 곳에서 나오는데도 화면은 0.1806, 문서는 18.1% 로 찍혔다.
> 계산을 한 곳에 모으는 것만으로는 안 된다 — **«보이는 값»을 만드는 자리도
> 하나여야 한다.** 서식 함수를 하나 두고 화면과 문서가 그것을 부르게 했다.

**★ 안 보이는 것은 검사에 안 걸린다**

> 그림을 넣는 함수가 아무것도 안 그렸는데 에러도 안 났다. 렌더 트리 검사는
> 통과했고, 실제 브라우저로 찍어 보고서야 빈자리를 봤다.
> 화면에서만 나는 흠은 화면을 봐야 보인다 — 통과했다는 사실이 사람을 안심시킨다.

## 2026-09-10 (다듬기) — 통과한 검사가 안 보는 곳

「완성도를 더 높여라」는 말을 받고 다시 열었다. 검사는 전부 통과하고 있었다.
그런데 통과한 자리 옆에서 넷이 나왔다. **검사가 본 것은 맞았고, 안 본 것이 틀렸다.**

**규칙 넷**

- **검사는 «검사한 것» 때문에만 죽어야 한다.** 결과를 찍다가 죽으면, 사람 눈에는
  검사가 실패한 것과 구분되지 않는다. 열 개 중 아홉이 그렇게 죽고 있었는데
  «실패 9건»으로만 보였다. 읽는 쪽을 고쳤다고 끝난 게 아니다 — **쓰는 쪽**을
  고쳐야 한다. 자식 프로세스도 자기 출력을 쓴다.
- **하나만 열어 보는 검사는 하나만 지킨다.** 만들 수 있는 것이 열여덟인데 검사가
  하나만 열어 봤다. 나머지 열일곱 중 여덟은 제목만 다르고 본문이 글자 하나
  안 다른 같은 문서였다. 검사는 **하나를 깊게** 보는 것과 **전부를 얕게** 보는 것,
  둘 다 있어야 한다. 둘은 다른 것을 잡는다.
- **주제를 안 보고 쓰면 주제가 없는 문서가 된다.** 자동으로 쓰는 문단이 «가장 큰
  것»만 보고 쓰면, 어느 주제를 골라도 같은 문장이 나온다. 고르게 해 놓고 고른
  것을 안 쓰는 것은, 고르게 하지 않은 것보다 나쁘다 — 고른 줄 알기 때문이다.
- **폭은 재 봐야 안다.** 종이에 얹었을 때 넘치는지는 문자열을 봐서 모르고, 화면을
  봐서도 모른다. 화면은 넓다. 종이 폭으로 놓고 오른쪽 끝을 재야 나온다.

**같은 값이 한 문서에 두 번 나오면 둘 중 하나는 틀렸다**

> 같은 이름의 값이 한 문서에서 다른 숫자로 두 번 나왔다. 둘 다 맞게 계산된
> 값이었다 — **세는 단위가 달랐다.** 하나는 «하나로 세는 것» 기준이고 하나는
> «벌어진 일» 기준이었다. 계산이 맞아도 단위가 다르면 한 문서에 못 싣는다.
> 지표마다 «어느 구간을 어느 단위로 재는가»를 표로 적어 두고, 문서와 단위가
> 다른 값은 아예 안 싣게 했다.

**손으로 쓴 숫자는 어디에 있든 어긋난다**

> 문서에 쓰는 숫자는 조회값과 대조하고 있었는데, 그 문서의 **원본이 되는 파일**은
> 대조하지 않고 있었다. 거기 손으로 적어 둔 두 숫자가 조회값과 달랐고, 그대로
> 인쇄돼서 같은 문서 안의 두 문단이 서로 다른 수를 말했다.
> 대조는 «문서»가 아니라 «손이 닿는 모든 자리»에 걸어야 한다.

**모든 것에 거는 규칙은 모든 것을 망가뜨릴 수 있다**

> 한 표에서 이름이 세로로 쪼개지는 것을 막으려고 «줄바꿈 금지»를 걸었다.
> 그 규칙이 **모든 표**에 걸려서, 첫 칸이 긴 문장인 표가 한 줄로 늘어나 종이
> 밖으로 나갔다. 좁게 고쳤어야 할 것을 넓게 고친 것이다.
> 서식 규칙은 «어디에 거는가»를 내용을 보고 정한다 — 내용을 못 보는 자리(서식
> 언어)에서 정하면, 안 맞는 곳에도 걸린다.

**안 그린 것을 설명하지 않는다**

> 그림에서 뺀 이유를 그림 아래에 적었는데, 그 칸은 애초에 그려지지도 않았다.
> 읽는 사람은 있지도 않은 것을 찾는다. 설명은 **보이는 것**에만 단다.

## 2026-09-11 (Day4) — 이 문서로 결정이 나오는가

어제까지는 **내가** 내 문서를 검사했다. 오늘 처음으로 **모르는 사람**이 읽었다.
내가 쓴 것을 내가 검사하면 내가 아는 것을 전제로 읽는다.

**규칙 여섯**

- **점수가 아니라 판정을 받는다.** 「몇 점」은 행동으로 안 바뀐다. 받을 것은 셋이다 —
  **판정**(셋 중 하나) · **사유**(문서의 어느 문장 때문인가) · **채택 조건**(무엇이
  있으면 채택하겠는가). 셋째 줄이 그대로 다음에 할 일이 된다.
- **독자를 「일반인」으로 잡으면 아무 지적이나 나온다.** 실제로 이 문서를 받을 사람을
  역할·관심사·거절 사유·아는 것 넷으로 적어 두고 그 사람으로 읽힌다.
  판정은 **그 문서를 만든 자리가 아닌 곳에서** 받는다. 만든 자리에서 물으면 이미 내 편이다.
- **독자가 둘이면 판정이 갈린다. 갈린 자리가 문서의 약한 곳이다.** 갈리지 않으면
  독자를 너무 비슷하게, 곧 너무 관대하게 만든 것이다.
- **지적을 받으면 먼저 셋으로 가른다** — 없는 것인가 · 있는데 못 찾은 것인가 ·
  내 사정을 모르고 한 말인가. **둘째가 가장 많이 나오고 가장 자주 놓친다.**
  못 찾은 것은 내용의 문제가 아니라 배치의 문제다. **더 쓰지 말고 앞으로 당긴다.**
  당긴 뒤 원래 자리에서는 지운다 — 같은 말이 두 곳에 있으면 둘 다 안 읽힌다.
- **지적을 다 받아들이면 제안이 사라진다.** 안 받아들인 것과 그 이유를 적은 것이
  이 문서의 답변이 된다. **전부 고쳤다면 판단을 안 한 것이다.**
- **판정을 올리려고 고쳐서는 안 되는 것이 있다.** 확신도를 올리는 것 · 확인 계획을
  지우고 단정문으로 바꾸는 것 · 전제 줄을 짧게 만드는 것. 셋 다 판정이 좋아지고
  셋 다 문서가 나빠진다. **확인 계획은 약점이 아니라 신뢰다.**

**★ 벤치마크가 무엇을 보장하지 않는가**

> 자동 채점기는 **문서가 잘 쓰였는지**를 잰다. **제안이 옳은지는 재지 못한다.**
> 근거가 진짜인지는 근거를 만들 때 확인하는 것이고, 채점은 그때 확인한 것을
> 다시 묻지 않는다. 점수를 올리려고 근거를 고치면 채점기만 속인다.
> 페르소나 판정도 마찬가지다 — 흉내 낸 독자이지 결정권자가 아니다.
> 이 둘이 갈리는 지점을 보는 것이 값어치이지, 어느 한쪽의 판정 자체가 답이 아니다.

**오늘 받은 판정과 내가 내린 결정**

| | |
| --- | --- |
| 고른 독자 | 실행을 승인하는 사람 → **보류** |
| 대신 세운 독자 | 방법의 허점을 보는 사람 → **기각** |
| 둘이 같이 짚은 것 | **앞 단계만 늘리는 제안 아닌가** — 조회해 보니 문서가 답할 수 있는 것이었다 |
| 갈린 이유 | 한쪽은 「되돌릴 수 있으면 해 보자」, 한쪽은 「변수 정의가 없으면 볼 필요 없다」 |
| 분류 | 고친다 11 · 위치를 옮긴다 2 · **안 고친다 4** |
| 보류를 채택으로 만들었나 | **아니다.** 없는 근거를 지어내는 것보다 「이것이 확인되면 채택해 달라」로 내는 편이 낫다 |

**안 고치기로 한 넷 — 이유가 곧 답변이다**

| 안 받아들인 지적 | 이유 |
| --- | --- |
| 불확실성을 다른 자로 재라 | 이 앱은 불확실성을 재는 자를 이미 하나 정해 두었고 모든 화면·문서가 그것을 쓴다. 여기서만 바꾸면 한 앱에 자가 둘이 된다. 대신 **다른 조건에서도 같은 방향인지**를 실었다 |
| 관측 구간을 더 짧게 잘라라 | 「값을 믿을 수 있는 구간」은 **임계값을 산출할 때** 쓰는 것이지 분석 기간이 아니다. 결과가 실제로 오기까지 걸린 날을 세어 보니 미성숙은 5.5%뿐이고, 빼면 격차가 **오히려 커진다** |
| 실험 설계가 없으면 환산값을 빼라 | 무작위 배정이 없다는 것은 이미 적어 두었고(그래서 그 절을 **아예 만들지 않았다**), 환산값에는 「추정 · 실측이 아니다」 라벨이 붙어 있다. 빼면 「얼마짜리 문제인가」에 답을 못 한다. 대신 **라벨이 안 읽히는 자리를 고쳤다** |
| 지금 비율이 의도인지 결과인지 | 결과다. 그래서 「이제부터 정하자」가 이 제안이다. 읽는 사람이 할 일은 같다 |

**옛 함정을 새 문서에 다시 대 본다**

지난주에 실측으로 걸렸던 함정 넷을 오늘 문서에 그대로 대 봤다. 넷 다 걸렸다.

| 함정 | 오늘 문서에서 |
| --- | --- |
| 집계 단위 | 단위는 박아 두었으나, **쪼개는 데 쓴 값이 다른 단위에 붙어 있었다.** 적어 두었다 |
| 가드레일 없는 성공 | **이 제안이 바로 주지표를 올리는 최단 경로였다.** 접는 기준에 뒤 단계 지표를 하나 더 넣었다 |
| 실험 설계 | 실험이 아니다. 다만 **접는 기준을 결과 보기 전에 적어 둔 것**이 같은 장치다. 조기 중단 규칙은 아직 없다 |
| 생존편향 | 이 데이터는 **통과한 것만 행이 있다** — 「떨어졌다」와 「아직 안 왔다」가 같은 모양이다. 결과가 오기까지 걸린 날을 세어 갈랐다 |

**★ 옛 규칙은 옛 문서에만 걸리지 않는다**

> 지난주에 배운 것을 지난주 산출물에만 대 보고 끝냈다. 오늘 같은 자를 새 문서에
> 대 보니 넷 다 걸렸다. **판단 기준은 쌓아 두는 목록이 아니라 새것이 나올 때마다
> 다시 대 보는 자다.** 대 보지 않으면 적어 둔 것과 하는 것이 갈린다.

**★ 「고쳤다」고 적은 것이 사실인지도 검사해야 한다**

> 답변지는 같은 지적이 다시 왔을 때 읽으라고 만든 파일이다. 그 안의 「고쳤습니다」가
> 문서와 갈리면 그 파일은 못 쓴다. 그래서 **분류표의 «고친다» 항목이 지금 문서에
> 실제로 있는지**를 기계가 본다. 사람이 쓰는 자리를 나중에 고치면 여기서 걸리는데,
> 그건 검사가 틀린 것이 아니라 **그 지적이 다시 열린 것**이다.

## 2026-09-11 (벤치마크) — 내가 만들지 않은 독자

같은 날 오후에 밖에서 판정이 하나 더 왔다. 내가 세운 독자 둘과 **겹치지 않는
자리**가 있었고, 거기가 하루 종일 아무도 안 본 자리였다.

**규칙 다섯**

- **내가 만든 독자는 내가 아는 것을 같이 안다.** 페르소나는 내가 써 준 배경으로
  읽는다. 그래서 「그래서 누가 하느냐」를 안 묻는다 — 물을 이유가 없기 때문이다.
  **밖에서 온 판정만 물을 수 있는 물음이 따로 있다.** 내가 만든 독자를
  아무리 까다롭게 만들어도 이 자리는 안 열린다.
- **판정이 여럿이면, 갈린 자리와 겹친 자리를 둘 다 본다.** 갈린 자리는 문서의
  약한 곳이고, **셋이 같이 짚은 것은 더 이상 의견이 아니라 구멍이다.**
- **점수는 항목마다 층으로 매겨진다.** 한 항목이 한 층 낮으면 그 항목 몫의
  4분의 1이 통째로 빠진다. 그래서 「전반적으로 조금씩 다듬는 것」은 점수를
  거의 안 올린다. **비어 있는 항목 하나를 채우는 것**이 올린다.
  어디가 비었는지는 항목별 점수를 보면 한눈에 나온다.
- **오해받은 것도 문서 탓이다.** 읽는 사람이 범위를 스스로 상상하면, 상상한
  범위의 위험을 문서에 청구한다. 「무엇을 바꾸는 결정인가」를 안 적으면
  안 바꾸는 것까지 위험으로 돌아온다. 범위는 한 줄이면 된다.
- **밖에서 온 지적에도 셋으로 가르는 것은 그대로다.** 다만 하나가 늘었다 —
  **없는 것을 쓰라는 지적은 안 받아들인다.** 모르는 것을 지어내면 그 자리에서
  무너지고, 채워 넣은 그 칸이 다음에 되짚을 때 제일 먼저 무너진다.

**★ 내가 아는 것이 문서에 없으면 문서는 그것을 모른다**

> 오늘 빠진 점수의 절반 넘게가 「누가 하느냐」와 「어느 것을 읽고 나온 값이냐」
> 두 가지였다. 둘 다 내가 아는 것이다. 아는 것이라 안 적었고, 안 적었으니
> 문서에는 없는 것이다. **나에게 당연한 것이 읽는 사람에게는 빈칸이다.**

**★ 검사는 내가 「있다」고 정한 것만 본다**

> 틀을 만들 때 항목을 셋으로 정하면 검사도 그 셋만 본다. 넷째 항목이 없으면
> 「넷째가 비었다」는 검사도 없다. **빈칸을 세는 검사는 빈칸을 만들지 못한다.**
> 그래서 검사를 다 통과해도 항목 자체가 빠진 것은 안 걸린다.
> 그건 밖에서 읽는 사람만 찾아 준다. 검사는 지키는 자이지 넓히는 자가 아니다.

**★ 채점기에 맞춰 고쳐도 되는 것과 안 되는 것**

> 가르는 물음은 하나다 — **이 고침이 문서를 더 참으로 만드는가, 더 그럴듯하게
> 만드는가.** 없던 것을 채우는 것은 앞쪽이고, 있던 단서를 지우거나 흐리는 것은
> 뒤쪽이다. 뒤쪽은 점수가 오르는 만큼 정확히 문서가 나빠진다.
> 고친 것을 다 적어 놓고 이 물음을 하나씩 대 보면 갈린다.
