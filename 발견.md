# 발견

조회 일시: 2026-09-09 18:42 (`python checks/w9d1_evidence.py`)
데이터: 구직 퍼널 (데이터 분석 직무) · 합성 데이터 · 기준일 2025-12-31

여기 있는 값은 전부 `core/metrics.py` 를 **직접 호출해서** 얻은 것이다.
8주차 리포트에서 옮겨 적은 것은 하나도 없다 — 리포트는 요약이고, 요약에는
분모가 빠져 있다. 요약을 요약하면 틀린 곳을 찾을 수 없게 된다.

**원인은 이 문서에 없다.** 오늘은 무엇이 다른지까지다.

---

## 발견 1 — 경쟁이 높은 공고에 넣은 지원의 서류 통과율이 낮다

경쟁이 높은 공고(경쟁도 0.65~1.00)에 넣은 지원의 서류 통과율이 **13.1%** 다.
경쟁이 낮은 공고(0.00~0.35)는 **23.4%** 다. 격차 **10.3%p**.
이것이 전체 지원 4,961건의 **26.5%** 를 차지한다.

| | |
| --- | --- |
| 값 | 13.1% (172건 / 1,316건) |
| 비교 대상 | 경쟁 낮음 (0.00~0.35) 23.4% (329건 / 1,403건) |
| 격차 | 10.3%p |
| 비중 | 26.5% — **이 구간에 진입한 지원 4,961건 중**. 전체 트래픽이 아니다 |
| 표본 | 가장 작은 칸 1,316건 · 한 건 흔들림 0.076%p (격차의 1/136) |
| 크기 | 격차 × 비중 = 2.75 · 절대 건수 환산 +137건 |

세는 단위는 **지원 1건**이다. 사람이 아니다 — 공고 경쟁도는 지원 건에 붙는 값이다.

## 발견 2 — 전환율이 가장 낮은 구간과 이탈이 가장 많은 구간이 다르다

지원자 1명 기준으로, 이탈 인원이 가장 많은 구간은 서류 통과 → 면접 통과로
**180명**이 빠진다. 전환율은 **56.3%** 다.
전환율이 가장 낮은 구간은 면접 통과 → 최종 합격으로 **45.7%** 이고,
빠지는 인원은 126명이다. 두 구간의 전환율 격차는 **10.6%p**,
이탈 인원 격차는 54명이다.
서류 통과 → 면접 통과에서 빠지는 180명은 전체 이탈 414명의 **43.5%** 이고,
첫 단계 지원자 520명의 **34.6%** 다.

| | |
| --- | --- |
| 값 | 서류→면접 56.3% (232명 / 412명) · 이탈 180명 |
| 비교 대상 | 면접→최종 45.7% (106명 / 232명) · 이탈 126명 |
| 격차 | 전환율 10.6%p · 이탈 인원 54명 |
| 비중 | 180명 = 전체 이탈 414명의 43.5% · 첫 단계 520명의 34.6%. **분모 둘을 다 적었다** — 하나만 적으면 발견 4(전체 이탈 대비 83.9%)와 견줄 수 없다 |
| 표본 | 분모 412명 · 한 건 흔들림 0.243%p |
| 크기 | 두 구간 모두 봐야 한다 — 구멍이 하나가 아니다 |

**둘이 다르면 구멍이 두 개다.** 비율만 보면 뒤 구간을,
인원만 보면 앞 구간을 고른다. 어느 쪽이 급한지는 이 데이터가 정하지 않는다.

건 기준으로는 두 구간이 같다 (지원 → 서류 통과). 그레인에 따라 병목이 옮겨
간다는 뜻이고, 그래서 발견에 세는 단위를 반드시 적는다.

## 발견 3 — 학력으로 갈리지만 손을 쓸 수 없다

학사의 서류 통과 경험률이 **74.9%** 다.
비전공 부트캠프는 **87.4%** 다. 격차 **12.5%p**.
학사가 전체 지원자 520명의 **49.0%** 를 차지한다.

| | |
| --- | --- |
| 값 | 74.9% (191명 / 255명) |
| 비교 대상 | 비전공 부트캠프 87.4% (97명 / 111명) |
| 격차 | 12.5%p |
| 비중 | 49.0% — 전체 지원자 520명 중 |
| 표본 | 가장 작은 칸 36건 · 한 건 흔들림 2.778%p. 격차(12.5%p)보다는 작지만 **분모 100 미만 칸이 둘**((미분류) 36건 · 전문학사 48건) → 그 두 칸은 비율로 쓰지 않고 건수로 쓴다 |
| 크기 | 격차 × 비중 = 6.12 (가장 큼) · 절대 건수 환산 +32명 |

**크기는 가장 큰데 개입할 수 없다.** 학력은 바꿀 수 있는 값이 아니다.
바꿀 수 있는 것은 학력별로 무엇을 다르게 할지이고, 그건 원인을 좁힌 뒤의 이야기다.
크기 표에서 1위라는 사실만으로 여기부터 파면 안 된다 —
그래서 크기 순서와 파고들 순서를 따로 적는다(아래 「크기와 순서」).

## 발견 4 — 건 기준으로는 첫 관문 하나가 압도적이다

지원 → 서류 통과의 전환율이 **18.1%** 다.
그 뒤 두 구간은 36.2% · 36.4% 다. 첫 구간과의 격차 **18.1%p**.
이 구간에서 빠지는 4,065건이 전체 이탈 4,843건의 **83.9%** 를 차지한다.

| | |
| --- | --- |
| 값 | 18.1% (896건 / 4,961건) |
| 비교 대상 | 서류→면접 36.2% (324건 / 896건) · 면접→최종 36.4% (118건 / 324건) |
| 격차 | 18.1%p (뒤 구간 대비) |
| 비중 | 이 구간 이탈 4,065건 = 전체 이탈 4,843건의 83.9% |
| 표본 | 분모 4,961건 · 한 건 흔들림 0.020%p |
| 크기 | 절대 건수로 가장 크다. 다른 어느 구간보다 한 자릿수 크다 |

발견 1과 같은 구간이다. 발견 1은 이 구간을 **축으로 쪼갠 것**이고,
발견 4는 이 구간이 **전체에서 얼마나 큰지**다. 둘은 겹치지 않는다.

## 발견 5 — 이 축들로는 갈리지 않는다

고용 형태 · 과제 유무 · 직무로 쪼개면 칸 사이 격차가 5%p 하한 아래다.

| 축 | 칸 | 격차 | 가장 작은 칸 | 한 건 흔들림 |
| --- | --- | --- | --- | --- |
| 고용 형태 | 5개 | 1.1%p | 553건 | 0.181%p |
| 과제 유무 | 2개 | 2.0%p | 1,936건 | 0.052%p |
| 직무 | 6개 | 3.2%p | 601건 | 0.166%p |
| 산업 | 11개 | 4.8%p | 266건 | 0.376%p |

**표본이 부족해서 못 가른 것이 아니다.** 세 축 모두 흔들림이 격차보다
한 자릿수 작다. 실제로 갈리지 않는다.

과제 유무는 비중이 큰데(과제 있음 39.0%) 격차가 작다 — 고칠 것이 없다.
산업은 격차가 4.8%p로 하한에 가깝지만, 결측 13.2%가 (미분류) 칸(655건, 13.2%)으로
빠져 있어 그 칸의 해석이 애매하다. 그래서 «갈리는 축» 후보로 올리지 않았다.

이것도 결과다. 내일 원인을 좁힐 때 **이 축들부터 지운다.**

---

## 못 쓰는 숫자

| 무엇 | 왜 못 쓰나 | 무엇이 채워지면 쓸 수 있나 |
| --- | --- | --- |
| 월 지원 건수 1.87 | **분모 불명.** 비율이 아니라 «지원자별 (건수 ÷ 관측 개월)»의 평균이다. 분자·분모가 없어 근거 문장을 만들 수 없다 | 못 채운다. 성질이 그렇다. 가드레일(움직였나)로만 쓴다 |
| 직무 적합도 0.511 | **분모 불명** + 지원 «건»의 평균이다. 많이 지원한 사람의 성향이 더 반영된다 (사람당 평균이 아니다) | 사람당 평균으로 다시 내면 «집단 간 비교»에는 쓸 수 있다. 오늘은 안 냈다 |
| 코호트 2025-10 (47명) | 관측 75일로 최소 90일에 못 미쳐 계산하지 않았다 | 그 코호트의 관측일이 90일을 넘으면 (2026-01 이후) |
| 코호트 2025-11 (49명) | 관측 43일로 최소 90일에 못 미쳐 계산하지 않았다 | 그 코호트의 관측일이 90일을 넘으면 (2026-02 이후) |
| 판정 카드 「시작 시점 · 유효 구간 밖」 (96명) | 관측 58일로 최소 90일 미달. 8주차에 무효로 판정해 지표를 계산하지 않았다 | 유효 구간(2025-09-30) 이후 시작분에 90일이 지나면 |
| 최근 3개월 서류 통과율 | 안 찬 구간이 섞여 있다. 12개월 평균은 포함 16.99% · 유효 구간만 16.22%로 **0.77%p** 벌어진다 | 유효 구간만 쓰는 것으로 정했다. 어느 쪽을 썼는지 반드시 적는다 |
| 학력 (미분류) 36건 · 전문학사 48건 | 분모 100 미만. 한 건이 2.08~2.78%p를 움직인다 | 비율로 쓰지 않고 건수로 쓴다 ("48명 중 37명") |
| 실험 효과 | **해당 없음.** 무작위 배정 실험이 없다. 관측 데이터뿐이다 | 실험을 하면. 이번 주 범위가 아니다 |

감춘 항목의 **값은 이 문서 어디에도 없다.** 조건과 건수만 적었다.
화면에서 감춘 값을 문서에 쓰면 8주차 목요일에 한 일이 없던 일이 된다.

## 확인한 정의

| | |
| --- | --- |
| 그레인 | 사람 퍼널 = 지원자 1명 · 분해 = 지원 1건. **발견마다 어느 쪽인지 적었다** |
| 분모 | 서류 통과율 = 지원 건 전체(구간 진입자) · 최종 합격률 = 지원자 전체 · 분해 비중 = 구간 진입 대상 전체 · 이탈률 = 판정 대상(성공 종료·판정 보류 제외) |
| 기간 | 2025-01-01 ~ 2025-12-31. 실제 이벤트 기간과 일치. 기준일 2025-12-31 고정 (현재 시각 안 씀) |
| 유효 구간 | ~2025-09-30 (관측 90일 이상 확보된 코호트). 10·11월 코호트는 계산하지 않았다 |

`CLAUDE.md` 에 적어 둔 정의 4건과 지금 코드가 하는 일을 대조했다 — **4건 다 일치**.
같은 대상이 두 번 세어지는가: `applications` 원본 5,035행에 키 중복 74건이 있고,
퍼널은 고유 키로 세므로 분모는 **4,961건**이다. 5,035가 아니다.

## 검증 경고가 내 근거에 주는 영향

| 경고 | 영향 | 어떻게 다뤘나 |
| --- | --- | --- |
| `applications.application_id` 중복 74건 | 있음 | 건 기준 분모를 4,961건으로 쓴다. 원본 행 수 5,035는 쓰지 않는다 |
| `applicants.education` 결측 6.9% (36/520) | 있음 | (미분류) 칸이 생긴다. 학력은 발견 3에만 쓰고 분모 100 미만 칸은 건수로 적었다 |
| `postings.industry` 결측 12.5% (30/240) | 있음 | 산업 축의 (미분류)가 커진다. 산업은 «갈리지 않는 축»으로만 적었다 |
| `applications.industry` 결측 13.2% (665/5,035) | 있음 | 같음. (미분류) 655건이 그 축에서 두 번째로 큰 칸이라 해석이 애매하다 |

편향 넷 중 둘이 걸렸다.
**응답률 편향** 없음 — 설문·만족도 컬럼이 없고 전부 행동 기록이다.
**가중 편향** 있음 — 직무 적합도가 건 평균이라 다작 지원자에 기울어 있다 (못 쓰는 숫자로 뺐다).
**결측이 분모에 남았는가** 없음 — `funnel_by()` 가 (미분류)로 분리하고, 분해 합계가
전체와 일치하는 것을 8주차 게이트 2에서 확인했다.
**비교가 깨진 것** 해당 없음 — 실험이 없다.

## 크기와 순서

크기는 계산이고, 순서는 사람이 정한다.

| 발견 | 격차 | 비중 | 격차 × 비중 | 절대 건수 | 손을 쓸 수 있나 |
| --- | --- | --- | --- | --- | --- |
| 3 학력 | 12.5%p | 49.0% | **6.12** | +32명 | ✕ 못 바꾼다 |
| 1 공고 경쟁도 | 10.3%p | 26.5% | **2.75** | +137건 | ○ 어디에 넣을지는 고른다 |
| 5 과제 유무 | 2.0%p | 39.0% | 0.78 | +39건 | △ 격차가 없다 |
| 5 직무 | 3.2%p | 17.0% | 0.55 | +27건 | △ 격차가 없다 |
| 5 산업 | 4.8%p | 8.8% | 0.42 | +21건 | △ (미분류)가 크다 |
| 5 고용 형태 | 1.1%p | 11.1% | 0.13 | +6건 | △ 격차가 없다 |

발견 2와 4는 축이 아니라 **구간**이라 이 표에 같은 자로 못 올린다.
발견 4의 절대 건수(4,065건)는 다른 어느 것보다 한 자릿수 크다.

**내일 파고들 순서 — 내가 정한 것**

1. **발견 1 (공고 경쟁도)** — 크기 2위지만 개입할 수 있는 유일한 축이다.
   표본도 가장 튼튼하다 (가장 작은 칸 1,316건, 흔들림이 격차의 1/136).
2. **발견 2 (구멍 둘)** — 어느 구멍이 급한지 정하려면 두 구간을 각각 쪼개 봐야 한다.
3. 발견 3 (학력) — 크기는 1위지만 못 바꾼다. 원인을 좁힌 뒤에 다시 본다.
4. 발견 5 — 내일 지울 축 목록으로 쓴다.

크기 1위를 1순위로 두지 않은 것이 오늘의 결정이다.
**격차 × 비중은 «얼마나 큰가»에 답하고, «무엇을 할 수 있는가»에는 답하지 않는다.**
