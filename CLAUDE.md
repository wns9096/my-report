# 이 앱에서 지킬 것

규칙 스무 개. 넘으면 지켜지지 않기 시작한다 — 넘으면 덧붙이지 말고 **줄인다.**
9주차 Day3 에 넷을 더하면서 스물하나가 되어, 원본 규칙 둘을 하나로 합쳤다.
다듬으면서 하나를 더했고(주제를 보고 쓴다), Day4 에 둘을 더하면서 겹치는 둘씩을
두 번 합쳤다 — 색 규칙, 사람이 쓰는 자리. 밖에서 판정을 받고 하나를 더하면서
「한 곳에서만 온다」 둘을 합쳤다. **지금도 스물이다.**
코드 스타일과 파일 구조는 안 적는다. 읽으면 알 수 있는 것은 규칙이 아니다.

## 세는 단위

- 사람 퍼널은 **사람 하나를 하나로** 센다 (`config.GRAIN`). 행을 세지 않는다.
- 분해는 지원 1건 단위다. **분해와 판정 카드는 그레인이 다르다.** 통일하지 마라.
- 비율을 낼 때 분모가 무엇인지 먼저 말해라. 이탈 분모에서 성공 종료는 뺀다.

## 원본이 어디인가

- 임계값·기간·축·색·표시 이름은 전부 `core/config.py` 하나다. 다른 파일에서
  새로 만들지 마라 — 16진수도 `(r,g,b)` 도 안 된다. 표시 이름을 바꿀 때도
  **원본 값은 고치지 마라.**
- 화면과 문서는 `core/context.py` 가 만든 값만 쓴다. 각자 계산하면 두 숫자가 갈린다.
- 제안서의 자동 절은 `제안카드.md` 를 옮기기만 한다. 화면은 카드를 읽기만 한다.
- 말도 검사도 한 곳에서만 온다 — 인쇄되는 말은 `config.PROPOSAL_WORDS`,
  인과 표현 검사는 `report/sections.py`. 절 제목도 분류어도 코드에 박지 말고,
  검사는 새로 만들지 말고 임포트해라. 둘로 두면 한쪽만 고쳐진다.

## 하지 말 것

- `data/` 안의 파일을 고치는 것. 데이터가 이상하면 검증 규칙으로 잡아라.
- 계산에 `datetime.now()` / `today()`. 기준일은 `config.AS_OF` 하나다.
- 판정을 계산 뒤에 두는 것. 순서는 **못 믿을 조건 → 주지표 → 가드레일** 이다.
- 사람이 쓰는 자리를 채우거나 늘리는 것. 비면 비운 채로 내보낸다. 자동 절에
  카드에 없는 값을 채우지 마라 — 없으면 «확인 필요»로 남긴다. 제안서에서
  사람이 쓰는 절은 **둘이다 — 위험·요청.** 셋 이상이면 자동화가 덜 된 것이고,
  하나도 없으면 책임질 사람이 없는 문서다.
- 채울 함수를 못 대는 절을 만드는 것. **자리를 비워 두는 것과 자리를 안 만드는
  것은 다르다** — 비워 두면 그 빈칸이 그대로 인쇄된다.
- 문장이 없는 그림을 넣는 것. **그림 하나에는 그것이 증명하는 문장 하나**가
  본문에 있어야 한다. 그림의 색은 셋까지 — 강조 1 · 기본 1 · 회색 1.
  그림이 강조하는 칸은 본문이 말하는 칸과 같아야 한다.
- **나에게 당연하다는 이유로 안 적는 것.** 누가 하는가 · 언제 하는가 ·
  얼마나 드는 일인가 · 어느 표를 읽고 나온 값인가가 제일 먼저 빠진다.
  카드에는 **담당·일정·공수**가, 머리글에는 **출처와 입구 검사 결과**가 있어야
  한다. 내가 아는 것이 문서에 없으면 **문서는 그것을 모른다.**
- 주제를 안 보고 자동 절을 쓰는 것. **고르게 해 놓고 고른 것을 안 쓰면,
  고르게 하지 않은 것보다 나쁘다** — 고른 줄로 알기 때문이다. 문장도 그림도
  분해도 그 주제의 구간·지표·단위를 따라간다. 단위가 다른 값은 안 싣는다.

## 지적을 받으면

- 먼저 셋으로 가른다 — **없는 것 · 있는데 못 찾은 것 · 해당 없는 것.**
  둘째가 가장 많이 나온다. 못 찾은 것은 내용이 아니라 배치의 문제라
  **더 쓰지 말고 앞으로 당긴다.** 당긴 뒤 원래 자리에서 지운다.
- **안 고친 것과 그 이유를 `제안서_답변지.md` 에 남긴다.** 전부 고쳤다면
  판단을 안 한 것이다. 판정을 올리려고 확신도를 올리거나 확인 계획을
  단정문으로 바꾸지 마라 — 확인 계획은 약점이 아니라 신뢰다.

## 나에게 시킬 때

- 내가 지정하지 않았는데 네가 정한 값이 있으면 **전부** 말해라. 값과 이유를 함께.
- 고치라고 한 파일 밖이 바뀌었으면 먼저 말해라.
- "판단하지 말고 사실만" 이라고 하면 판정을 붙이지 마라.

---

## 참고 — 제안서의 절 (규칙이 아니라 표다)

절을 바꿀 때 여기부터 고친다. 절을 더하려면 «채우는 함수» 칸을 먼저 채울 수
있어야 한다. 못 채우면 그 절은 없는 것이다.

| 절 (인쇄되는 제목) | 답하는 질문 | 채우는 것 | 누가 |
| --- | --- | --- | --- |
| 지금 어디서 가장 많이 빠집니까 | 전환율이 가장 낮은 구간은 어디이고 거기서 몇 건이 빠지는가 | `metrics.funnel()` | 자동 |
| 누구에게서 벌어집니까 | 그 구간을 한 축으로 쪼개면 어느 칸과 어느 칸이 갈리는가 | `metrics.funnel_by()` — **그 주제의 구간으로** + 못 믿을 조건 | 자동 |
| 얼마짜리 문제입니까 | 그 격차가 이어지면 한 해에 몇 건인가 | 격차 × 분모 ÷ 기간 · `metrics.monthly()` | 자동 |
| 무엇을 하자는 것입니까 | 무엇을 중단하고 무엇을 다시 보고 무엇을 실행하는가 | `제안카드.md` 중 주제키가 맞는 카드 — 무엇을·담당·일정·공수·비용·효과·되돌림 | 자동 |
| 이 판단이 틀릴 수 있는 지점 | 어디서 틀린 것이고 언제 접는가 | — | **사람** |
| 무엇을 결정해야 합니까 | 지금 내려야 하는 결정은 무엇인가 | 규모·결정 선택지 셋은 자동, 문장은 사람 | **사람** |

**이 도메인에는 없어서 만들지 않은 절**

| 안 만든 절 | 왜 |
| --- | --- |
| 실험 결과 | 무작위 배정이 없다. 관측 데이터뿐이라 개입 효과를 못 낸다 |
| 경쟁사 비교 | 데이터에 없다 |
| 금액 환산 | 건당 금액을 적어 둔 항목이 없다. 건수로만 낸다 (단가 미확보) |

머리글 두 줄은 자동이다 — 첫 줄은 기간·기준일·세는 단위·표본, 둘째 줄은
`metrics.data_source()` 가 낸 출처와 입구 검사 결과다. 경고 건수는 **감추지
않는다.** 경고 0인 데이터가 아니라 경고를 세어 두고 넘긴 데이터다.

임계값·추세 주제는 구간이 아니라 지표를 다룬다. 그때 첫 절은 «이 지표가 지금
어디에 있고 어느 쪽으로 움직이는가»에 답한다 — 절 이름은 같아도 **답하는 질문이
다르면 부제도 다르다.** 지표를 어느 단위로 재는지는 `metrics.METRIC_SPAN` 하나에
적혀 있고, 문서와 단위가 다른 값은 싣지 않는다.

순서를 바꾸지 않는다. 규모를 제안 뒤에 두면 «왜 이걸 해야 하지»가 안 풀린 채로
제안을 읽고, 위험을 요청 뒤에 두면 결정한 다음에 리스크를 듣는다.
